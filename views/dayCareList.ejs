<html lang="en">
<head>
    <% include ./partials/head %>
</head>
<body class="container-fluid">

    <div id="header" class="navbar navbar-default navbar-fixed-top">
    
   <% include ./partials/header %>
     </div>
<div id="wrapper">
  <div id="sidebar-wrapper" class="col-md-2">
     <% include ./partials/sidebar %>
   </div>
   <div id="main-wrapper" class="col-md-10 pull-right">
        <div id="main">
            <div class="page-header">
                <h3>Daycare List</h3>
                <%
                if(typeof  success_message.addsuccess != 'undefined' ){%>
                 
                 <div class="alert alert-success">
                    <strong>Success!</strong> successfully Added Day cares.
                 </div>
                <%}
                %> 
                <spna style="float:right;margin-top: -3em;"><a href="add-daycare"><button type="button" class="btn btn-primary">Add</button></a></span>
            </div>
            <div class="data-table">
              <table id="day_care_list" class="display" cellspacing="0" width="100%">
                <thead>
                    <tr>
                        <th>Sr.No.</th>
                        <th>Name</th>
                        <th>Email</th>
                        <th>Mobile</th>
                        <th>No. of children</th>
                        <th>Action</th>
                    
                    </tr>
                </thead>
                <tbody>
                     <% for(var i=0; i<dayCares.length; i++) { %>
                      <tr>
                          <td><%= (i+1) %></td>
                          <td><%= dayCares[i].name %></td>
                          <td><%= dayCares[i].email %></td>
                          <td><%= dayCares[i].mobile %></td>
                          <td><%= dayCares[i].count %></td>
                          <td><a href="uid_daycare/?uid_daycare=<%= dayCares[i].id%>"><button type="button" class="btn btn-info">View</button></a></td>
                      </tr>
                  <% } %>  
                </tobdy>    
              </table>  
            </div>  
        
        </div>

        

  </div>
</div>
</body>
<% include ./partials/footer %>
</html>